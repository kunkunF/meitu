// url模块
// 一开始使用加数据，将来换成真数据以后只需要在这一个地方修改baseUrl
define(() => {
    return {
        rapBaseUrl: "http://rap2api.taobao.org/app/mock/178206/",
        // phpBaseUrl: " http://localhost/meitu/api/v1/"
    }
});